---
paths:
  - "tests/**"
---

# Test suite

Loaded when working with tests. Root context: [CLAUDE.md](../../CLAUDE.md). Lint and test with the exact invocations in [.github/workflows/ci.yml](../../.github/workflows/ci.yml), never a self-chosen scope.

## Test Count
All 1743 tests as of 2026-08-22, of which 1741 pass locally: the two sleep-back wiring tests in tests/test_scripts/test_run_healthcheck.py (`test_failing_check_cannot_sleep_away_its_own_alert`, `test_healthy_check_gets_past_the_alert_condition`) can only pass between 20:00 and 08:00 local, because sleep_back.ps1 checks its sleep window before the conditions those tests assert on and exits at the window check during the day. Pre-existing and filed as #150; they are Windows-only so CI never sees them. Issue #149 adds 21 cross-platform tests, 14 in tests/test_governance/test_pruner.py (hour slicing as a pure function, multi-slice deletes, interrupted-prune durability, the per-run row budget) and 7 in tests/test_db/test_migration_0010.py, whose plan assertions check for a SEARCH rather than only naming the index, because the defect being pinned is a full child-table scan (issue #123 adds 11 cross-platform tests in tests/test_pipeline/test_normalize.py, which goes 24 -> 35: every one of the eight pre-existing `_fetch_rolling_stats` tests seeded `market_observations_normalized` and had to be rewritten against the rollup, and the new fixtures go through the real `upsert_item_rollup()` rather than hand-written `daily_rollup_item` rows, because the whole argument for reading the rollup is that it aggregates the same rows under the same filter and a hand-written fixture could drift off that silently. The additions are parity against the pre-change query and against statistics.fmean/pstdev, an EXPLAIN QUERY PLAN pin that the prefetch seeks `daily_rollup_item` and never names `market_observations_normalized` (migration-0009 shape; it is the only test that can tell the two implementations apart once both return the same numbers), the calendar-day window edge against an injected clock, six `_check_rollup_freshness` cases, and one end-to-end `_execute` run whose single pending row could not get a z-score from batch stats; issue #113 adds 4 cross-platform tests in tests/test_db_isolation.py; issue #104 adds 20 cross-platform tests in tests/test_backup/test_verify.py; issue #105 adds 9 in tests/test_reporting/test_health.py, 2 in tests/test_cli/test_check_data_health_cli.py, and 1 Windows-only in tests/test_scripts/test_run_healthcheck.py; issue #78 adds 43 Windows-only, 41 in tests/test_scripts/test_sleep_back.py and 2 sleep-back wiring tests in test_run_healthcheck.py; issue #117 adds 1 cross-platform test in tests/test_viz/test_backend.py). Seventy-eight of them (5 run_hourly.bat lock-guard tests from issue #3, 4 run_daily.bat freshness-gate tests from issue #12, 11 run_healthcheck.bat alerting + sleep-back-wiring tests from issues #4/#105/#78, 13 setup_tasks.bat registration + wake-to-run + backup-task tests from issues #6/#40/#80, 4 run_backup.bat tests from issue #80, and 41 sleep_back.ps1 tests from issue #78, all in tests/test_scripts/) are Windows-only and skip on the Linux CI runners, so CI reports 1665 passed + 78 skipped, green on Python 3.11 and 3.12. Two issue-#78 tests (the condition 2 quiet case and the wake check sitting behind it) check their own precondition and skip when input arrives mid-check, so a local run while the operator is at the keyboard reports up to 2 skips rather than 1722 passed; the negative case (input detected, sleep refused) is fully deterministic and is the load-bearing one. tests/test_viz/conftest.py pins `matplotlib.use("Agg")` (issue #117): nothing in the package sets a backend, so Windows picks TkAgg and every figure the suite builds spins up a Tcl/Tk interpreter no test displays, which flaked four different viz tests one at a time under full-suite load; headless CI already falls back to Agg, so tests/test_viz/test_backend.py is a real regression test on Windows and a weak one there. Issue #80 also adds 15 durable-backup tests (tests/test_backup/) and 9 backup-freshness health-check tests (tests/test_reporting/test_health.py), all cross-platform. Issue #43 adds 73 cross-platform tests: 47 in tests/test_ingestion/test_cloud_sync.py, 14 in tests/test_db/test_cloud_sync_queries.py, 11 in tests/test_cli/test_sync_snapshots_cli.py, plus 1 smoke case. Issue #95 adds 3 more in that file (TestNaiveUtcClockNormalization), the only tests that run the sync stage with the real aware clock instead of an injected naive one. CLI `--help` assertions must check only "Usage:" (test_cli_smoke.py convention): rich colorizes and wraps the options table, so option names are not contiguous in `result.output` at the CI runner's terminal width. The learning track adds 147 cross-platform tests: 107 in tests/test_learning/ (20 scheduler, 18 store, 23 loader, 24 session, 22 bank integrity) and 40 in tests/test_cli/test_learn_cli.py. Every test in tests/test_learning/ inherits an autouse fixture pinning `WOWFC_LEARN_DB` to tmp_path, and the CLI tests pass it through CliRunner's `env=`, so a test run never writes `data/learn/` into the checkout. The same guarantee covers the product DB since issue #113: the autouse `isolated_product_db` fixture in tests/conftest.py pins `WOW_FORECASTER_DB_PATH` to tmp_path for EVERY test, because a test that builds a real pipeline stage without an override inherits AppConfig's default `db_path` and writes to the real database (TestMissingCredentials persisted a failed `sync_snapshots` run row on every suite run for weeks, and the rows read as genuine operational failures). The failure mode is silent, so tests/test_db_isolation.py asserts the fixture is doing its job; a test that needs a specific DB still overrides via CliRunner `env=` or its own monkeypatch.
